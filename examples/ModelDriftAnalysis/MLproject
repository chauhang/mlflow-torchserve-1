name: Model-Drift-Analysis-Base-Model

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      max_epochs: {type: int, default: 3}


    command: |
          python base_model.py \
            --max_epochs {max_epochs}


